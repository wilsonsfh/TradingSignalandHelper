# GitHub Publication and README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the completed credential-free trading assistant to `wilsonsfh/TradingSignalandHelper` with a trustworthy, motivation-first README and durable personal-wiki evidence.

**Architecture:** Rewrite only documentation copy while preserving verified code behavior. Clean generated SQLite sidecars, run the complete offline/SDK/design gates, review the full diff for secrets and release risks, then commit and push the existing feature branch.

**Tech Stack:** Markdown, Python/pytest, Flask, vanilla JS/CSS, git, GitHub CLI, personal Obsidian wiki.

## Constraints

- Preserve existing private repository visibility.
- Do not claim profitability, financial advice, or live-money readiness.
- Lead with motivation and benefits, then exact quick-start and operating steps.
- Explain who should and should not use it.
- Keep mock mode the default; REAL mode remains out of scope.
- Include 152-test evidence and the real SDK 10.8.6808 contract boundary.
- Scan the full diff for credentials/state files before commit.
- Commit and push only the intended project files to the existing feature branch.
- Update the personal wiki with repository URL, branch, commit SHA, verification, and remaining OpenD gate.

## Tasks

1. Ignore/remove generated SQLite WAL/SHM sidecars.
2. Rewrite `README.md`: motivation, benefits, audience, features, quick start,
dashboard workflow, modes, architecture, safety, Moomoo, webhook, configuration,
verification, limitations, roadmap.
3. Run tests, compile, JS syntax, pip check, detector, SDK contract, and diff check.
4. Review status/diff for secrets and generated files.
5. Commit with an accurate phase-completion message and push the feature branch.
6. Verify GitHub branch/commit URL and checks.
7. Add immutable raw publication evidence plus source/project/index/log wiki updates.
8. Return a terminal-friendly reference covering setup, use, internals, safety, and next steps.
